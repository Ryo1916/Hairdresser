<!DOCTYPE html>
<html>
  <head>
    <%= render 'shared/head' %>
  </head>
  <body class="d-flex flex-column">
    <%= render 'layouts/navbar' %>
    <main>
      <div class="container-fluid <%= controller_name %> <%= action_name %> <%= mt-0 if current_page?(root_path) %>">
        <%= yield %>
      </div>
    </main>
    <%= render 'layouts/footer' %>
  </body>
</html>
